import React, { Fragment } from "react";
import NavBar from "./../../components/NavBar/NavBar";

const Tag = (props) => {
  return (
    <Fragment>
      <NavBar />
      <div>estamos en tags</div>
    </Fragment>
  );
};

export default Tag;
